version: 2.1

orbs:
  win: circleci/windows@2.2.0 

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    executor:
      name: win/default
      version: stable
      shell: cmd.exe
    steps:
      - checkout
      - run: echo Hello World!
          
      
