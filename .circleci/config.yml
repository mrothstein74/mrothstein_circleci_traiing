version: 2.1

orbs:
  win: circleci/windows@2.2.0 

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    executor:
      name: win/default
      version: edge
    steps:
      - checkout
      - run: git clone git@github.com:beatcracker/VSCELicense.git
      - run: ls 
      - run: ls VSCELicense
     
