version: 2.1 

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    machine:
      image: windows-server-2022-vs2022:1646182006
      resource_class: windows.large
    steps:
      - checkout
      - run: python --version
      - run: py --list
          
      
