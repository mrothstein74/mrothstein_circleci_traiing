version: 2.1

orbs:
  win: circleci/windows@2.2.0 

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    executor:
      name: win/default
      version: edge
      shell: cmd.exe
    steps:
      - checkout
      - run:
          name: build
          shell: cmd.exe
          command: bat\bat1.bat
      
