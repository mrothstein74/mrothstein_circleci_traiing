version: 2.1 

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    machine:
      image: 'windows-server-2022-vs2022:edge'
      resource_class: windows.large
      shell: powershell.exe -ExecutionPolicy Bypass
    steps:
      - checkout
      - run: msbuild -t:Restore;Build Fibonacci.csproj
      
          
      
